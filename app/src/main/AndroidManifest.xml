<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pathsocial">

    <!-- Required for 28 and below. -->
    <uses-permission android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION" />
    <!-- Required for 29+. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PathSocial">
        <activity
            android:name=".FollowActivity"
            android:exported="true" />
        <activity
            android:name=".LeagueActivity"
            android:exported="true" />
        <activity
            android:name=".MyTrailActivities"
            android:exported="true"
            android:windowActionBar="false" />
        <activity
            android:name=".RecordActivity"
            android:exported="true" />
        <activity
            android:name=".RegisterMoreDataActivity"
            android:exported="true" />
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".LoginActivity"
            android:exported="true" />
        <activity
            android:name=".MapPage"
            android:exported="true" />
        <activity
            android:name=".RegisterTrailActivity"
            android:exported="true" />
        <activity
            android:name=".RegisterTrailActivityFollowed"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>